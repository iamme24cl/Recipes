<div class="col-md-4">
  <% if recipe.image.attached? %>
    <%= image_tag url_for(recipe.image), class: "img-responsive img-rounded" %>
  <% end %>
  
  <small class="recipe-title-content">
    <b>Submitted by:</b> <%= recipe.user.name %> <%= submitted_on recipe %>  
  </small>				
</div>
